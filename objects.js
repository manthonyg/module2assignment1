const students = ["michael", "nicholas", "katie", "mark", "nicole"];

console.log(students[0]); // the first student in the list
console.log(students[1]); // the second student in the list

console.log(students.length); // the length of the students array

const grades = [90, 95, 50, 88, 70];

console.log(grades[0]); // first grade
console.log(grades[1]); // second grade

console.log(grades[grades.length - 1]); // last grade
console.log(grades[grades.length - 2]); // second to last grade
